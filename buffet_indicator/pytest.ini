[pytest]
addopts = -q
testpaths = tests
markers =
    integration: hits real network -- opt in via INTEGRATION_TESTS=1
    acceptance: end-to-end Spec v4 acceptance tests -- opt in via ACCEPTANCE=1
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
