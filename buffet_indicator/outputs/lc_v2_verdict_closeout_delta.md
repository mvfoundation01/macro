# Phase F-DOC closeout re-run delta analysis

**Timestamp**: 2026-05-25T23:35Z
**Closeout HEAD commit**: `623e09f` (Phase F-DOC.A: requirements.lock pinned)
**BLK-1 baseline commit**: `88ea428` (Phase F-BLK1 progress report; verdict authored under `22f2cad`)
**Sealed pre-reg**: UNCHANGED at SHA-256 `c3c3ec1a83e4cb9cf8f7c35523f0542530cfc4bb2e986ae49ddab23c1bed8b05` (immutable)

---

## Pinned environment

| Library | Sealed pinned (§3.7.2 / §3.8) | Installed in closeout env | Match? |
|---|---|---|---|
| Python | 3.12.x recommended | **3.12.10** | ✓ |
| `arch` | 7.0.0 | 7.0.0 | ✓ |
| `pandas` | 2.2.3 | 2.2.3 | ✓ |
| `numpy` | 1.26.4 | 1.26.4 | ✓ |
| `scipy` | 1.13.1 | 1.13.1 | ✓ |
| `statsmodels` | 0.14.2 | 0.14.2 | ✓ |

Closeout venv: `$TEMP\macro_v2_pinned_venv` (ephemeral; not committed). Installed via `uv pip install --require-hashes -r requirements.lock` against the hashed lock generated by `uv pip compile --generate-hashes --python-version 3.12`. 1092-line lock file pins every transitive with cryptographic hashes.

---

## SHA-256 comparison

| Verdict | File-byte SHA-256 | Normalized SHA-256 (substantive) |
|---|---|---|
| BLK-1 baseline (`outputs/lc_v2_verdict_blk1_baseline.json`) | `df542640992d4cf5b6014d6483629266f93399dd01d3d9f7cc9a181ea507ab0c` | `0fe5c5053af78bac061a7ca89568b484b6583a6d9da0d0ddbf5b0837d7344f02` |
| Closeout re-run (`outputs/lc_v2_verdict_closeout.json`) | `1925e658ef9c88aabecae03c445396f4ed6ffe7a290f07cd0ecb5122a5c31899` | `0fe5c5053af78bac061a7ca89568b484b6583a6d9da0d0ddbf5b0837d7344f02` |
| **Match (normalized)** | — | **YES** |
| Match (file-byte) | NO (expected — dynamic fields differ) | — |

The file-byte SHA difference is expected: the closeout re-run was on a different host time, different `git_head` commit, and under different Python version. The **normalized** SHA — which strips `run_timestamp`, `git_head`, `_meta.library_versions_installed`, `_meta.python_version`, `_meta.platform`, and the corresponding `_meta.seal_metadata.*` fields — is **byte-identical** across BLK-1 and closeout, proving substantive equivalence.

---

## Verdict outcome

| Item | BLK-1 | Closeout (pinned) | Δ |
|---|---|---|---|
| `verdict` | `FAIL` | `FAIL` | **UNCHANGED** |
| `n_pass_total` | `1/7` | `1/7` | **UNCHANGED** |
| `n_pass_predictive` | `0` | `0` | **UNCHANGED** |
| `evidence_status` | `MIXED` | `MIXED` | **UNCHANGED** |
| `retest_status` | `NOT_APPLICABLE` | `NOT_APPLICABLE` | **UNCHANGED** |
| `data_cutoff` | `2026-03-31` | `2026-03-31` | **UNCHANGED** |

---

## Per-criterion deltas (canonical statuses + values)

| # | Criterion | BLK-1 status (value) | Closeout status (value) | Δ |
|---|---|---|---|---|
| C1 | OOS R² @ 1Y on LC_TIER2 > 0.005 | `NOT_EVALUABLE_COUNTED_FAIL` (—) | same (—) | UNCHANGED |
| C2 | OOS R² @ 3Y on LC_TIER2 > 0.020 | `NOT_EVALUABLE_COUNTED_FAIL` (—) | same (—) | UNCHANGED |
| C3 | OOS R² @ 5Y on LC_TIER2 > 0.040 | `NOT_EVALUABLE_COUNTED_FAIL` (—) | same (—) | UNCHANGED |
| C4 | LC_FULL \|t_NW\| > 1.65 any horizon | `NOT_EVALUABLE_COUNTED_FAIL` (—) | same (—) | UNCHANGED |
| C5 | ADF rejects all 5 (Holm-Šidák α=0.05) | `FAIL_STATISTICAL` (max p ≈ 0.7648) | same (identical to 14 sig figs) | UNCHANGED |
| C6 | max VIF < 5.0 | `PASS` (≈ 1.6951) | same (identical) | UNCHANGED |
| C7 | Bonferroni any p < 0.0025 | `NOT_EVALUABLE_COUNTED_FAIL` (—) | same (—) | UNCHANGED |

---

## Field-level diffs (substantive content only, tol = 1e-12)

**0 substantive field differences** across the entire normalized verdict JSON.

This includes:
- All per-cell regression β, α, t_NW, p_NW, se_NW_β
- All Stambaugh, Campbell-Yogo status enums
- All OOS R² + Clark-West statistics
- All bootstrap CI bounds (50 000 reps, deterministic seeded)
- All skew-t fits + fallback metadata
- All ADF p-values
- All VIF values + max
- All 20 Bonferroni cells
- The PIT look-ahead audit (`audit_status=PASS`, 12 cells × 756 origin-cell pairs, 0 violations)

---

## Library version delta (acceptable / blocking)

Per §4.4 tolerance rules:

| Category | Count | Acceptable? |
|---|---|---|
| Floating-point drift < 1e-12 | 0 | yes |
| Floating-point drift 1e-12 to 1e-4 | 0 | yes (would investigate if present) |
| Drift > 1e-4 in regression coefficients | 0 | n/a |
| ADF p-value crossing 0.05 | 0 | n/a (would callback) |
| Max VIF crossing 5.0 | 0 | n/a (would callback) |
| Criterion status changes | 0 | n/a (would callback) |
| Verdict outcome flip | 0 | n/a (would HALT) |
| **Callback triggers fired** | **0** | — |

The numpy `1.26.4` → `2.4.4`, scipy `1.13.1` → `1.17.1`, statsmodels `0.14.2` → `0.14.6` family of changes (Phase E off-pin → BLK-1 off-pin → closeout pinned) does NOT introduce any detectable substantive drift in the v2.0 verdict. Codex Round 5's prediction (pinned re-run = same verdict) is empirically confirmed at zero substantive diff.

---

## Reproducibility claim

Post-closeout, the v2.0 verdict is **reproducible under sealed-pinned library versions** at substantive byte-equivalence (normalized SHA-256 `0fe5c5053af78bac061a7ca89568b484b6583a6d9da0d0ddbf5b0837d7344f02`). The file-byte SHA differs by run only in documented dynamic-metadata fields (timestamp, host, env), which is the expected and intended behavior of Phase F-BLK1.F's byte-exact writer.

The verdict can be claimed **environment-clean for external scrutiny** (SSRN submission, code review, audit). Any independent reproducer using:

1. The sealed pre-reg at SHA-256 `c3c3ec1a…`
2. The pinned `requirements.lock` (1092 lines, hash-pinned)
3. The frozen `outputs/lc_v2_verdict.json` provenance chain
4. The data cutoff `2026-03-31`
5. Python 3.12.x

will produce a verdict JSON with the SAME normalized SHA-256 `0fe5c5053af…` and the SAME outcome `FAIL (1/7)`.

---

## Conclusion

Phase F-DOC.C **closeout re-run PASSES** the substantive-equivalence test against the BLK-1 canonical. The pinned-environment verdict is byte-identical to the off-pin BLK-1 verdict at the level of all numerically-substantive fields. Zero callback triggers fired. The v11.4 sprint's verdict is now environment-clean and reproducible.
